<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/styles.css" media="screen" title="no title" charset="utf-8">
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Baloo+Bhai|Shrikhand" rel="stylesheet">
  </head>
  <body>
    <header>
      <div class="sign-out">
        {{#if user}}
        <form action="/signout?_method=DELETE" method="POST">
          <button type="submit" name="submit" class="signout">Sign Out</button>
        </form>
        {{else}}
        {{/if}}
      </div>
      <h1 class="main-header"><a href="/users">Custom Closets</a></h1>
    </header>
    <div class="container">
      {{{body}}}
    </div>
  </body>
</html>
