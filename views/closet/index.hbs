<h1>{{user.username}}'s Closet</h1>

{{#each user.clothes}}
  <ul>
    <li>{{category}}<img src="{{imageURL}}" /></li>
  </ul>
{{/each}}

<a href="/closet/{{user._id}}/new">Add clothes to your closet!</a>
